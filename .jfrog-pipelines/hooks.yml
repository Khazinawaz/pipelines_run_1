pipelines:
  - name: RunHooksPipeline2
    postRun:
      execution:
        onExecute:
          - printenv
          - echo "Executing Post Run"
    preRun:
      execution:
        onExecute:
          - printenv
          - echo "Executing Pre Run"
          - echo 'changed'
    steps:
      - name: A1
        type: Bash
        execution:
          onExecute:
            - printenv
            - echo "Step A"
      - name: B2
        type: Bash
        configuration:
          inputSteps:
            - name: A1
        execution:
          onExecute:
            - printenv
            - echo "Step B"
      - name: B3
        type: Bash
        configuration:
          inputSteps:
            - name: A1
        execution:
          onExecute:
            - printenv
            - echo "Step B"
