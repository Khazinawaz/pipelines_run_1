pipelines:
  - name: RunHooksPipeline2
    preRun:
      execution:
        onExecute: 
          - echo 'hello'
    steps:
      - name: A1
        type: Bash
        execution:
          onExecute:
            - printenv
            - echo "Step A"
      - name: A2
        type: Bash
        configuration:
          inputSteps:
            - name: A1
        execution:
          onExecute:
            - printenv
            - echo "Step A"
      - name: B2
        type: Bash
        configuration:
          inputSteps:
            - name: A1
        execution:
          onExecute:
            - printenv
            - echo "Step B"
